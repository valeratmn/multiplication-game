/**
 * Модуль конфигурации игры
 * Содержит все настройки приложения
 */

import type { GameConfig } from './types.js';

/**
 * Конфигурация игры
 * Все основные параметры игры на умножение
 */
export const CONFIG: Readonly<GameConfig> = {
  /** Множитель для всех примеров (таблица умножения на 4) */
  multiplier: 4,
  
  /** Длительность анимации появления кубиков (в миллисекундах) */
  animationDuration: 500,
  
  /** Длительность показа сообщения о неправильном ответе (в миллисекундах) */
  wrongAnswerDuration: 1000,
  
  /** Общее количество примеров для решения */
  totalExamples: 10
} as const;
